import { convergeB, midHelper } from './internal.js';

/**
 * Conventional wire-*-handlers.ts entry (handler heuristic).
 */
export function wireEventHandlers(): number {
	/*cursor:wire-handlers*/
	return midHelper();
}

export function wireConverge(): number {
	return convergeB();
}
