export const simpleConst: string;
export function simpleFunction(param: number): boolean;
export interface SimpleInterface {
  id: number;
  name: string;
}
export type SimpleType = 'a' | 'b' | 'c';
export class SimpleClass {
  constructor(value: string);
  getValue(): string;
}
