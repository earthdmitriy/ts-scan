# tests

All tests in this repository should use real file operations against the `samples/` folder when fixtures are needed.

This ensures the resolver and related tools are validated against real files and search behavior instead of relying on mock command output or synthetic fixtures.
